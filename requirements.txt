# ============================================================
#  triangle-pixel 依赖清单
#  按模块分组, 标 [core] 的是必装, 其余按需.
# ============================================================

# ---- [core] 核心引擎 (三角采样/重建/渲染) ----
Pillow>=10.0
numpy>=1.24

# ---- [opt] Numba 加速 (process_fast 路径, ~54x) ----
# 解除下行注释可启用:
# numba>=0.58

# ---- [opt] 真实 SSIM 计算 (BENCHMARK/PAPER 数字需要) ----
# 解除下行注释可获得符合论文的 SSIM:
# scikit-image>=0.21

# ---- [opt] benchmark / comprehensive_benchmark / generate_figures ----
# matplotlib>=3.7
# scipy>=1.10

# ---- [opt] 训练 (train_tri.py) ----
# torch>=2.0
# torch-geometric>=2.4

# ---- [opt] 数据集转换 (tri_convert.py) ----
# tqdm>=4.65
# opencv-python>=4.7

# ---- [opt] 可视化 (generate_figures.py) ----
# matplotlib>=3.7

# ============================================================
# 推荐完整安装 (除深度学习训练外):
# ============================================================
# pip install Pillow numpy numba scikit-image matplotlib scipy tqdm opencv-python